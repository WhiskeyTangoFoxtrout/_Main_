build(*Skel):
    -Main serves(*) only then we branch diff main for diffrent pipeln and resources:
    -clientle gone have usr input:
        )could get a subsystem
    -"Hood"->surface level part#*piston spark plugs type things*#:
        +"unda" the hood is where it gets or bit and byte-y at. Actual programs dats basically the setting`#*mite have configs of configs*#,
        its meant to handle streams/core function/method/modules/obj/,
        the usr need but dont need to program ln:#want to make a gui for dis reason make it a lil easier to customize
            & SecRing here #*a lil encrpyt*#
