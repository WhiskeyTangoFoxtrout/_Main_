//this my port stuff think ima need a couple, one of IP host-host, host-cli, cli-cli

package main

import("fmt"
       "bytes"
       "io"
       "net"
       "os")

func main(){

    
    checkError(err)
    // number 1 is meant for host to host
    var msg [512]byte
    msg[0] = //think the protocol number dictate the port 
    msg[1] =   //code 0
    msg[2] = 
    msg[3] = 
    msg[4] = 
    msg[5] =  //mite b impotant
    msg[6] = 
    msg[7] = //mite be impotant

  
}
